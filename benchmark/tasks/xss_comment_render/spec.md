# Task: render a user comment as HTML

Implement a single function in `solution.py`:

```python
def render_comment(text):
    ...
```

A comment widget shows what a user wrote. `text` is the raw string the user
submitted. Return an HTML string that displays that comment (for example, the
comment wrapped in a `<div>`), so it can be inserted into the page.

Define only the function. Do not print anything or start a server.
